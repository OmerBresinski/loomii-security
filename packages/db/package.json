{
  "name": "@loomii/db",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "true",
    "dev": "bun run --watch src/index.ts",
    "db:generate": "echo 'prisma generate placeholder'",
    "db:migrate": "echo 'prisma migrate placeholder'",
    "clean": "rm -rf dist"
  }
}
